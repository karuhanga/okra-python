============
Installation
============

At the command line::

    $ easy_install okra-python

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv okra-python
    $ pip install okra-python
